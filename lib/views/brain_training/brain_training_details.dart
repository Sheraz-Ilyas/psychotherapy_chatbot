import 'package:flutter/material.dart';

class BrainTrainingDetails extends StatelessWidget {
  const BrainTrainingDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Brain Training Details"),
      ),
    );
  }
}
